{
  "name": "panel-marketing",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "wrangler pages dev public",
    "deploy": "wrangler pages deploy public --project-name=panel-marketing"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
